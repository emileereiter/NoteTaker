{
  "name": "note-taker",
  "version": "1.0.0",
  "description": "Note Taker application using JSON as a storage container.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Emilee Reiter",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.3",
    "mysql": "^2.18.1",
    "uuid": "^3.4.0"
  }
}
